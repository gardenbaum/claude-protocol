# Node
node_modules/
package-lock.json

# Local bootstrap output (not templates)
.claude/agents/
.claude/hooks/
.claude/skills/
.claude/settings.json
.claude/settings.local.json
CLAUDE.md
# Rules are committed (dev rules for this project itself)
!.claude/rules/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
.pytest_cache/
.coverage
htmlcov/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test outputs
/tmp/
.history/

# bv (beads viewer) local config and caches
.bv/

# Beads orchestration
.beads/
.worktrees/
# bd auto-export can stray to worktree root in rare cases — never commit it
/issues.jsonl

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Local install artifacts (this repo uses templates/ as source; .claude/ is a local install target)
/.claude/.manifest.json
/.claude/.upgrades/
beads_orchestrator_errors.log
